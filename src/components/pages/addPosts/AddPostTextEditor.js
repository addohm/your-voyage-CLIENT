import TextEditor from "../../textEditor/TextEditor"

export default function AddPostTextEditor({ defaultValue, className }) {
    return (
        <TextEditor defaultValue={defaultValue} className={className} />
    )
}
